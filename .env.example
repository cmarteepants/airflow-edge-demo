AIRFLOW_JWT_SECRET=change-me-to-a-random-string
AIRFLOW_UID=50000

# IP address of this machine, reachable from the Pi (e.g. LAN IP, VPN IP, Tailscale IP)
EDGE_HOST_IP=192.168.1.100

# Demo scripts config
AIRFLOW_URL=http://localhost:8081
AIRFLOW_USER=admin
AIRFLOW_PASS=admin
PI_HOST=airflow-demo
